---
title: "disease_information script"
format: html
---
```{r}
setwd ("~/Desktop/DCDMgroup10")

getwd
```
```{r}
data <-read.delim("disease_information.txt", header = TRUE, stringsAsFactors = FALSE)

head(data)
```
```{r}
str(data)
```
```{r}
summary(data)
```
```{r}
nrow(data)
ncol(data)
```
```{r}
colSums(is.na(data))
```
No missing values 
Next, check for duplicate rows in the dataframe

```{r}
duplicated(data)
sum(duplicated(data))
```
Confirmed that there are no duplicate rows 

Next, checking if there are duplicates in the DO.disease.ID
There are 1562 meaning multiple rows share the same disease ontology

```{r}
sum(duplicated(data$DO.Disease.ID))
```


